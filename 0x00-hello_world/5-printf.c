#include <stdio.h>

/**
 * main - prints the output to the user
 *
 * Return: 0 for proper exit, and non-zero on otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
